"use client";
type Product={id:number;name:string;price:number;memberPrice?:number|null;icon:string;category:string;color:string};type Discount={id:string;name:string;percent:number;active:boolean};
const money=(n:number)=>n.toLocaleString("de-DE",{style:"currency",currency:"EUR"});
export function PricingPanel({products,setProducts,discounts,setDiscounts}:{products:Product[];setProducts:(p:Product[])=>void;discounts:Discount[];setDiscounts:(d:Discount[])=>void}){return <section className="panel pricing-panel"><div className="panel-head"><div><p className="eyebrow">PREISE & RABATTE</p><h2>Mitgliedspreise</h2></div><button onClick={()=>setDiscounts([...discounts,{id:crypto.randomUUID(),name:"Neuer Rabatt",percent:10,active:false}])}>+ Rabatt</button></div><div className="member-prices">{products.map(p=><label key={p.id}><span>{p.icon} {p.name}<small>Normal {money(p.price)}</small></span><input type="number" step="0.5" placeholder="Mitgliedspreis" value={p.memberPrice??""} onChange={e=>setProducts(products.map(x=>x.id===p.id?{...x,memberPrice:e.target.value===""?null:Number(e.target.value)}:x))}/></label>)}</div><h3>Rabattaktionen</h3><div className="discount-rules">{discounts.map(d=><div key={d.id}><input value={d.name} onChange={e=>setDiscounts(discounts.map(x=>x.id===d.id?{...x,name:e.target.value}:x))}/><input type="number" min="0" max="100" value={d.percent} onChange={e=>setDiscounts(discounts.map(x=>x.id===d.id?{...x,percent:Number(e.target.value)}:x))}/><span>%</span><button className={d.active?"on":""} onClick={()=>setDiscounts(discounts.map(x=>({...x,active:x.id===d.id?!x.active:false})))}>{d.active?"Aktiv":"Aus"}</button></div>)}</div></section>}
